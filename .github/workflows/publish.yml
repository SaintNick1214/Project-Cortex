name: Publish to npm

on:
  push:
    branches:
      - main
  # No paths filter - let version detection decide whether to publish

# Prevent parallel execution with Python tests to avoid Convex deployment conflicts
concurrency:
  group: cortex-tests-${{ github.ref }}
  cancel-in-progress: false

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      sdk-version-changed: ${{ steps.check-sdk.outputs.changed }}
      sdk-new-version: ${{ steps.check-sdk.outputs.version }}
      wizard-version-changed: ${{ steps.check-wizard.outputs.changed }}
      wizard-new-version: ${{ steps.check-wizard.outputs.version }}
      python-version-changed: ${{ steps.check-python.outputs.changed }}
      python-new-version: ${{ steps.check-python.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check SDK version
        id: check-sdk
        run: |
          # Get current version from package.json
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "📦 SDK version: $CURRENT_VERSION"

          # Get published version from npm (or 0.0.0 if not published yet)
          NPM_VERSION=$(npm view @cortexmemory/sdk version 2>/dev/null || echo "0.0.0")
          echo "📚 npm version: $NPM_VERSION"

          # Compare versions (publish if package.json > npm)
          if [ "$CURRENT_VERSION" != "$NPM_VERSION" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
            echo "✅ New SDK version detected: $NPM_VERSION → $CURRENT_VERSION"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "⏭️  SDK already published at $CURRENT_VERSION, skipping"
          fi

      - name: Check create-cortex-memories version
        id: check-wizard
        run: |
          # Get current version from package.json
          CURRENT_VERSION=$(node -p "require('./packages/create-cortex-memories/package.json').version")
          echo "📦 Wizard version: $CURRENT_VERSION"

          # Get published version from npm (or 0.0.0 if not published yet)
          NPM_VERSION=$(npm view create-cortex-memories version 2>/dev/null || echo "0.0.0")
          echo "📚 npm version: $NPM_VERSION"

          # Compare versions (publish if package.json > npm)
          if [ "$CURRENT_VERSION" != "$NPM_VERSION" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
            echo "✅ New wizard version detected: $NPM_VERSION → $CURRENT_VERSION"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "⏭️  Wizard already published at $CURRENT_VERSION, skipping"
          fi
      
      - name: Check Python SDK version
        id: check-python
        run: |
          # Get current version from pyproject.toml
          cd cortex-sdk-python
          CURRENT_VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['project']['version'])")
          echo "📦 Python SDK version: $CURRENT_VERSION"
          
          # Get published version from PyPI (or 0.0.0 if not published yet)
          pip install --upgrade pip > /dev/null 2>&1
          PYPI_VERSION=$(pip index versions cortex-memory 2>/dev/null | grep "cortex-memory" | head -1 | sed 's/.*(\(.*\))/\1/' || echo "0.0.0")
          echo "📚 PyPI version: $PYPI_VERSION"
          
          # Compare versions (create tag if pyproject.toml > PyPI)
          if [ "$CURRENT_VERSION" != "$PYPI_VERSION" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
            echo "✅ New Python SDK version detected: $PYPI_VERSION → $CURRENT_VERSION"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "⏭️  Python SDK already published at $CURRENT_VERSION, skipping"
          fi

  publish-sdk:
    needs: check-version
    if: needs.check-version.outputs.sdk-version-changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Deploy Convex backend
        run: npx convex deploy --yes
        env:
          CONVEX_DEPLOYMENT: ${{ secrets.CONVEX_URL }}
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}

      - name: Generate Convex types
        run: npx convex codegen
        env:
          CONVEX_URL: ${{ secrets.CONVEX_URL }}
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}

      - name: Run tests
        run: npm test
        env:
          CONVEX_URL: ${{ secrets.CONVEX_URL }}
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Build package
        run: npm run build

      - name: Verify package contents
        run: npm pack --dry-run

      - name: Publish to npm
        run: npm publish --access public --provenance --ignore-scripts
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create Git Tag
        run: |
          VERSION=${{ needs.check-version.outputs.sdk-new-version }}
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v$VERSION" -m "Release v$VERSION"
          git push origin "v$VERSION"

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ needs.check-version.outputs.sdk-new-version }}
        run: |
          gh release create "v$VERSION" \
            --title "v$VERSION" \
            --notes "## Release v$VERSION

          See [CHANGELOG.md](https://github.com/SaintNick1214/Project-Cortex/blob/main/CHANGELOG.md) for details.

          ### Quick Start
          \`\`\`bash
          npm create cortex-memories
          \`\`\`

          ### Install SDK Only
          \`\`\`bash
          npm install @cortexmemory/sdk@$VERSION
          \`\`\`"

      - name: Verify npm publish
        run: |
          echo "Waiting for npm registry to update..."
          sleep 10
          npm view @cortexmemory/sdk version
          echo "✅ SDK published successfully!"

      - name: Post-publish summary
        run: |
          VERSION=${{ needs.check-version.outputs.sdk-new-version }}
          echo "════════════════════════════════════════════════════════════"
          echo "  ✅ SDK PUBLISHED!"
          echo "════════════════════════════════════════════════════════════"
          echo ""
          echo "📦 Package: @cortexmemory/sdk@$VERSION"
          echo "📚 npm: https://www.npmjs.com/package/@cortexmemory/sdk"
          echo "🐙 GitHub: https://github.com/SaintNick1214/Project-Cortex/releases/tag/v$VERSION"
          echo ""

  publish-wizard:
    needs: check-version
    if: needs.check-version.outputs.wizard-version-changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: |
          cd packages/create-cortex-memories
          npm install

      - name: Build package
        run: |
          cd packages/create-cortex-memories
          npm run build

      - name: Run smoke tests
        run: |
          cd packages/create-cortex-memories
          ./test-smoke.sh

      - name: Verify package contents
        run: |
          cd packages/create-cortex-memories
          npm pack --dry-run

      - name: Publish to npm
        run: |
          cd packages/create-cortex-memories
          npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Verify npm publish
        run: |
          echo "Waiting for npm registry to update..."
          sleep 10
          npm view create-cortex-memories version
          echo "✅ Wizard published successfully!"

      - name: Post-publish summary
        run: |
          VERSION=${{ needs.check-version.outputs.wizard-new-version }}
          echo "════════════════════════════════════════════════════════════"
          echo "  ✅ WIZARD PUBLISHED!"
          echo "════════════════════════════════════════════════════════════"
          echo ""
          echo "📦 Package: create-cortex-memories@$VERSION"
          echo "📚 npm: https://www.npmjs.com/package/create-cortex-memories"
          echo ""
          echo "🚀 Try it: npm create cortex-memories"
          echo ""

  trigger-python-publish:
    needs: check-version
    if: needs.check-version.outputs.python-version-changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required to create and push tags
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Need full history for tagging
      
      - name: Create and push Python SDK tag
        run: |
          VERSION=${{ needs.check-version.outputs.python-new-version }}
          TAG_NAME="py-v$VERSION"
          
          echo "════════════════════════════════════════════════════════════"
          echo "  🏷️  CREATING PYTHON SDK TAG"
          echo "════════════════════════════════════════════════════════════"
          echo ""
          echo "📦 Python SDK version: $VERSION"
          echo "🏷️  Tag: $TAG_NAME"
          echo ""
          
          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Create annotated tag
          git tag -a "$TAG_NAME" -m "Python SDK Release v$VERSION"
          
          # Push tag (this triggers publish-python.yml)
          git push origin "$TAG_NAME"
          
          echo ""
          echo "✅ Tag $TAG_NAME created and pushed!"
          echo "🚀 This will trigger the Python SDK publish workflow"
          echo ""
